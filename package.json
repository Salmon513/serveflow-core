{
  "name": "serveflow-core",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build:shared": "pnpm --filter @serveflow/shared build",
    "build:prompts": "pnpm --filter @serveflow/prompts build",
    "build:backend": "pnpm --filter @serveflow/backend build",
    "build:frontend": "pnpm --filter @serveflow/frontend build",
    "build": "pnpm build:shared && pnpm build:prompts && pnpm build:backend && pnpm build:frontend",
    "typecheck": "pnpm build:shared && pnpm build:prompts && pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.9.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["@nestjs/core", "esbuild", "sharp"]
  }
}
